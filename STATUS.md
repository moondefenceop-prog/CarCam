# 진행 상황 (2026-08-12)

새 세션에서 이어받기 위한 인수인계 문서. 앱의 목적과 설계 제약은 [CLAUDE.md](CLAUDE.md),
인식 파이프라인의 근거는 [ml/README.md](ml/README.md)에 있다.

브랜치 `claude/sharp-gates-liiv51`, 기기 갤럭시 S8(SM-G950N, Android 9)에서 검증.

---

## 1. 이번 작업의 큰 줄기

시작점은 `154러7070`이 인식되지 않는 문제였고, 파고들다 보니 **거의 모든 "오분류"가 실제로는
잘못된 크롭**이었다는 결론에 도달했다. 모델을 바꾸는 시도(합성 폰트, 실제 번호판 폰트, 거부
클래스, 모아레 증강, 혼합 학습)는 전부 **실전 성적이 오히려 나빠졌고**, 점수를 올린 것은 전부
"모델에게 무엇을 보여주는가"를 고친 것이었다.

오프라인 평가셋 기준 진행:

| 단계 | 점수 |
|---|---|
| 출발점 (등분할 슬롯 + 확신도 스윕) | 18/28 |
| 골짜기 스냅 + 확신도 스윕 제거 | 21/28 |
| 테두리 여백 제거 | 25/28 |
| Otsu 이진화 + 배경 제거 | 26/28 |
| 번호판 탐지 버그 3건 수정 | 28/28 |
| 구형 2줄 번호판 지원 | 29/30 |
| 구멍(닫힌 자음) 검증 | **30/30** |

**반드시 기억할 것: 합성 검증 정확도는 실전 성능을 전혀 예측하지 못한다.** 모든 학습 변형이
합성 val 97~98%인데 실제 번호판에서는 17/28~31/31로 갈렸다.

---

## 2. 앱에 실제로 들어간 것

### 인식
- `util/GlyphPreprocess.kt` — OpenCV 없이 포팅한 전처리. 적분영상 적응형 이진화, Otsu,
  골짜기 기반 글자 분할, 배경 제거, 기울기 보정(shear), 플러드필 구멍 계수, 연결요소 기반
  글자줄 박스 도출.
- `util/GlyphClassifier.kt` — 40클래스 TFLite. **ML Kit이 한글을 읽었을 때도 검증**하고
  확신도 0.90 이상이면 교정한다(예전에는 한글을 전혀 못 읽었을 때만 동작).
- 모델 에셋 `glyph_cnn.tflite` = `ml/cnn_otsu.tflite` (Otsu 이진화로 학습).

**실기기 측정(21~23장):** 교정 2건(`214허4167`→`214머4167`), **깨진 것 0건.**

### 기능
- 직접 입력 조회 — 인식이 안 될 때 번호를 쳐서 등록 여부 확인, 미등록이면 그 자리에서 등록.
- 엑셀/CSV 가져오기 — `.xlsx`를 ZIP+XML로 직접 파싱(POI 미사용), BOM·빈 셀 처리.
- 입출차 기록 — 확인/입출차 모드, 번호판별 90초 쿨다운, 되돌리기·출차 변환, CSV 내보내기.
- 검증 캡처 — 한 번 탭으로 프레임 저장(타이핑 없음).
- `util/InterstitialPolicy.kt` — 전면 광고 타이밍 규칙(SDK 미연결, 단위 테스트만 존재).

### 데이터
- Room v3. `plates`(등록 목록) + `visits`(입출차).
- **수천 대 대응**: `digitsKey` 컬럼 + 인덱스로 두 조회 경로 모두 인덱스 질의.
  5000대 실측 — 등록 7.8ms, **미등록 14.3ms**, 숫자폴백 13.0ms (프레임 간격 300ms).
- 마이그레이션 1→2, 2→3 모두 직접 작성. 파괴적 마이그레이션 금지: 등록 목록은 사용자가
  엑셀로 넣은 것이라 날아가면 복구 불가.

---

## 3. 지금 열려 있는 작업 (여기서 이어서)

### (A) 검증 캡처가 실패를 재현하지 못함 — 방금 수정, 미검증
저장 경로가 프레임을 **JPEG 품질 90으로 재압축**하고 있었다. ML Kit은 원본 YUV를 받으므로
저장 파일은 ML Kit이 본 것이 아니다. 실제로 촬영 당시 `620로0097`, `581호4774`로 틀렸던
프레임을 저장본에서 다시 돌리면 ML Kit이 `도`, `우`로 **정확히** 읽는다.

`ImageUtils.imageProxyToUprightBitmapExact()`(YUV→ARGB 직접 변환)를 추가하고 캡처 경로를
그것으로 바꿨다. **아직 기기에서 검증하지 않았다.**

- 다음 할 일: 설치 후 같은 장면을 다시 캡처해서, 저장본이 촬영 당시의 오인식을 재현하는지 확인.
- 재현되면 JPEG 가설이 확인되는 것이고, 그때부터 초성 오인식을 제대로 파고들 수 있다.
- **주의: 이 가설은 아직 검증되지 않았다.** 손실 압축이 인식을 돕는다는 것은 화면 촬영의
  모아레를 저역통과로 지우기 때문이라는 설명이 가능하지만, 확인 전에는 단정하지 말 것.

### (B) 초성 오인식 3건 (실기기 캡처, 정답은 사람이 판독)
| 정답 | 촬영 당시 앱 | 오류 |
|---|---|---|
| 620**도**0097 | 620**로**0097 | ㄷ→ㄹ |
| 581**우**4774 | 581**호**4774 | ㅇ→ㅎ |
| 444**아**4313 | 444**가**4313 | ㅇ→ㄱ |

저장본에서는 앞의 둘을 ML Kit이 정확히 읽어 재현되지 않는다(→ (A) 해결 후 재수집 필요).
CNN은 저장본에서 `도`(0.55), `우`(0.62)로 정답을 내지만 0.90 미만이라 교정은 발동하지 않는다.

### (C) ML Kit 글자줄 박스가 용도기호를 빠뜨리는 경우 — 별개 과제
`10버7399_1.png`, `10버7399_2.png`는 ML Kit이 `10허7399`로 읽으면서 박스는 번호판 뒷부분만
덮는다. 글자가 박스 밖이라 이후 어떤 크롭·기울기 보정·분류기 변경으로도 닿을 수 없다.
`MiddleVerificationTest`에서 제외했고 파일은 기록으로 남겼다. 해결하려면 보고된 박스 밖까지
연결요소를 다시 모아 진짜 글자줄을 복원해야 한다(`ml/eval_real.py`가 하는 일).

### (D) 광고
`InterstitialPolicy`만 있고 AdMob SDK는 미연결. 노출 시점은 세션 종료 / 목록 일괄 작업
직후로 한정하고, 조회 중·시작 시에는 절대 띄우지 않는 것이 설계.

---

## 4. 작업 방식 (지키지 못해 지적받은 것들)

- **결과 보고할 때 실패 이미지를 항상 첨부한다.** 케이스 번호·정답·예측·모델이 실제로 본
  크롭까지. 텍스트 표만으로는 크롭 문제인지 탐지 문제인지 분류 문제인지 구분되지 않는다.
- **다음 실험을 시작하기 전에 결과부터 보고한다.**
- **분류기가 실제로 본 그림을 확인한다.** `GlyphClassifier.debugCapture` + `DumpCropTest`.
  크롭을 따로 재구현해서 들여다보다가 모델이 본 적 없는 그림을 보고 엉뚱한 방향으로 간 적이
  한 번 있다.
- **사용자가 테스트 중일 때 설치하지 않는다.** `installDebug`와 `am instrument`는 실행 중인
  앱을 강제 종료시켜 크래시처럼 보인다.
- **테스트는 절대 실제 DB에 닿지 않는다.** 두 계측 테스트가 `clearAllTables()`로 사용자
  데이터를 지우고 있었고, 실제로 기기에서 실행됐다. 지금은 저장소가 DB를 주입받고 테스트는
  인메모리를 쓴다.

---

## 5. 개발 환경 메모

- Gradle 래퍼 없음. `/c/Users/breye/.gradle/wrapper/dists/gradle-8.9-bin/*/gradle-8.9/bin`을
  PATH에 넣고 `gradle` 직접 실행.
- Git Bash가 adb 경로를 변환하므로 `MSYS_NO_PATHCONV=1` 필요.
- `connectedAndroidTest`는 끝나고 앱을 제거한다. 파일을 남기려면
  `installDebug installDebugAndroidTest` 후 `am instrument`로 실행할 것.
- 진단용 테스트는 `@Ignore`. 실행하려면 임시로 떼고 `am instrument`.
- 캡처 회수: `adb pull /sdcard/Android/data/com.carcam.platecheck/files/verify`

## 6. 테스트 현황

| 테스트 | 내용 | 상태 |
|---|---|---|
| `MiddleVerificationTest` | 중간 글자 교정, 회귀 금지 게이트 | 통과 (교정 2, 파손 0) |
| `MigrationTest` | v1→v3, v2→v3 데이터 보존 | 통과 (4) |
| `VisitRecordingTest` | 쿨다운·토글·되돌리기 | 통과 (9) |
| `LookupScaleTest` | 5000대 조회 속도 | 통과 (3) |
| `InterstitialPolicyTest` | 광고 빈도 한도 | 통과 (5, 단위) |
| `PlateRecognitionBenchmarkTest` | 종단 벤치마크 | 최고 61.5% exact / 82.1% digit |
| `DumpCropTest` | 진단 전용 | `@Ignore` |
